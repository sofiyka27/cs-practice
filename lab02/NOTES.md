рабочая папка (working directory) — где ты видишь и правишь файлы;

индекс (staging area) — промежуточная зона, куда ты добавляешь изменения перед коммитом;

коммит — сохранённая «точка» истории с изменениями и сообщением.



Git и GitHub: в чём разница

Git — это локальная система контроля версий (работает у тебя на компьютере, хранит историю, ветки, коммиты);

GitHub — это хостинг для репозиториев (место, где ты публикуешь свой Git-репозиторий, чтобы делиться им и работать в команде).



Использованные команды

git status — показывает текущее состояние: какие файлы изменены, какие добавлены в индекс, какая ветка активна и есть ли неотправленные коммиты.

git diff — сравнивает рабочую папку и индекс (изменения, которые ещё не добавлены).

git diff --staged — сравнивает индекс и последний коммит (изменения, готовые к коммиту).



Ветки

Ветка — это указатель на коммит; она позволяет вести параллельные линии разработки.

При новом коммите текущая ветка «сдвигается» на этот новый коммит.

Слияние (merge) берёт изменения из одной ветки и добавляет их в другую.

